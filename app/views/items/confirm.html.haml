.ConfirmBox
  .Purchase
    .Purchase--text 購入を確定しますか？

  .Item
    .Item--image
      = image_tag(@image.image.url, class: "main__photo")
    .Item--price
      .Item--price__base
        = @item.price.to_s(:delimited)
        円
      .Item--price__tax 
        %h3 送料込み
  .Point
    %h2 ポイントなし
  .Payment
    .Payment--text
      %h2 支払金額
    .Payment--price
      = @item.price.to_s(:delimited)
      円
    .Paymnt--status
    = @item.auction_status


  = form_tag(action: :pay, method: :post) do
    %script.payjp-button{:src => "https://checkout.pay.jp", :type => "text/javascript" ,"data-text" => "購入する" ,"data-key" => "pk_test_209b52d522239a08ceda7909"}

