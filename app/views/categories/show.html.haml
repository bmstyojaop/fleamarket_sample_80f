.CategoryWrapper
  .CategoryWrapper__links
    - @category_links.each do |category|
      = link_to category.name, category_path(category)
.ItemsContainer
  .ItemsContainer__index
    .ItemsContainer__index--title
      = "#{@category.name}の商品一覧"
      .title__border
    - if @items
      = render "items/item", items: @items
    = paginate @items